const a = 1;
if (a > 0) {
  const log = require("./character.js");
  log();
}

// let myChars = characters;

// myChars = staelRing(myChars, "Bilbo");

// for (c of characters) {
//   console.log(c);
// }

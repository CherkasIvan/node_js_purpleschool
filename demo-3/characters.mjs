export const characters = [{ name: "Frodo" }, { name: "Bilbo" }];

export function greet(character) {
  console.log("Поздравляю" + " " + character);
}

export default function log() {
  console.log("log");
}
